<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erivan.santos.contasapp">

    <application
        android:name=".ApplicationCustom_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.ContaDetalheActivity_" android:label="Detalhes da conta"></activity>
        <activity android:name=".Activity.AddContaActivity_" android:label="Adicionar uma conta"/>
        <activity android:name=".Activity.ListaActivity_" android:label="Contas"/>
        <activity android:name=".Activity.NovoUsuarioActivity_" android:label="Adicionar um usuário"/>
        <activity android:name=".Activity.LoginActivity_" android:label="Login"/>
        <activity android:name=".MainActivity_" android:label="Home"/>
        <activity android:name=".Activity.LogoActivity_" android:label="Logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>